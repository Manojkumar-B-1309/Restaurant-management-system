--Query 3 Top most cuisine sold

SELECT CUISINE, SALES FROM 
(SELECT M.FOOD_CUISINE AS CUISINE, SUM(O.QUANTITY) AS SALES 
FROM MENU M INNER JOIN ORDERS O ON M.FOOD_ID = O.FOOD_ID GROUP BY CUISINE ORDER BY SALES DESC)
WHERE ROWNUM <= 1;
