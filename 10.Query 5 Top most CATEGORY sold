--Query 5 Top most CATEGORY sold

SELECT FOOD_CATEGORY, SALES FROM 
(SELECT M.FOOD_CATEGORY AS FOOD_CATEGORY, SUM(O.QUANTITY) AS SALES 
FROM MENU M INNER JOIN ORDERS O ON M.FOOD_ID = O.FOOD_ID GROUP BY FOOD_CATEGORY ORDER BY SALES DESC) 
WHERE ROWNUM <= 1;
