--Query 7 Date WHERE SALES LOWEST

SELECT DATE_TIME, QUANTITY FROM 
(SELECT TRUNC(DATE_TIME) AS DATE_TIME, SUM(QUANTITY) AS QUANTITY 
FROM ORDERS GROUP BY TRUNC(DATE_TIME) ORDER BY QUANTITY ASC) 
WHERE ROWNUM <= 1;
