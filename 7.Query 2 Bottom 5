--Query 2 Bottom 5

SELECT FOOD_NAME, SALES FROM 
(SELECT M.FOOD_NAME AS FOOD_NAME, SUM(O.QUANTITY) AS SALES 
FROM MENU M INNER JOIN ORDERS O ON M.FOOD_ID = O.FOOD_ID GROUP BY M.FOOD_NAME ORDER BY SALES ASC)
WHERE ROWNUM <= 5;
