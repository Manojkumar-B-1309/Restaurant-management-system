--Query 4 Top most TYPE sold

SELECT FOOD_TYPE, SALES FROM 
(SELECT M.FOOD_TYPE AS FOOD_TYPE, SUM(O.QUANTITY) AS SALES 
FROM MENU M INNER JOIN ORDERS O ON M.FOOD_ID = O.FOOD_ID GROUP BY FOOD_TYPE ORDER BY SALES DESC) 
WHERE ROWNUM <= 1;
