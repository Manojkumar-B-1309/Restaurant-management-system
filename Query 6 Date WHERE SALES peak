--Query 6 Date WHERE SALES peak

SELECT DATE_TIME, QUANTITY FROM (SELECT TRUNC(DATE_TIME) AS DATE_TIME, SUM(QUANTITY) AS QUANTITY FROM ORDERS GROUP BY TRUNC(DATE_TIME) ORDER BY QUANTITY DESC) WHERE ROWNUM <= 1;
